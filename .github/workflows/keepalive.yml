name: Keep app active

on:
  schedule:
    - cron: '17 3 */3 * *'
  workflow_dispatch:

jobs:
  ping-health:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render and Supabase health endpoint
        run: curl --fail --show-error --silent https://g1-test-unofficial.onrender.com/health
